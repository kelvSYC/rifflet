package com.kelvsyc.rifflet.civ3

/**
 * The 2 values of [ContEntry.type].
 *
 * Ordinal position matches the raw file value directly (raw `0` is [WATER], raw `1` is [LAND]) —
 * do not reorder these constants.
 */
enum class ContType { WATER, LAND }
